import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Checkout",
}
export default function CheckooutPage() {
    return (
        <div>
            checkout
        </div>
    );
}